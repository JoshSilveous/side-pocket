/**
 * (Unused) Per-path config now lives directly in SidePocketNeon.tsx:
 *   - colours        → the PATH_COLORS map
 *   - brightness     → the built-in power-on animation (+ the powerOn() handle)
 *
 * This file is no longer imported anywhere and is safe to delete.
 */
export {};
